{
  "name": "mongodb-kit",
  "devDependencies": {
    "@types/bun": "^1.3.14"
  },
  "private": true,
  "scripts": {
    "dev": "bun --filter '*' dev",
    "build": "bun --filter '*' build",
    "check": "bun --filter '*' check",
    "test": "bun test"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@modelcontextprotocol/inspector": "^0.22.0"
  }
}